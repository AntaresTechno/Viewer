# View backend — FastAPI + Python
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
SQLAlchemy[asyncio]>=2.0
aiosqlite>=0.20
PyJWT>=2.8
httpx>=0.27
beautifulsoup4>=4.12
soupsieve>=2.5
lxml>=5.2
cssselect>=1.2
jsonpath-ng>=1.6

# HEIC/HEIF 封面在浏览器（尤其 Windows）打不开，需要服务端解码成 JPEG 再下发。
pillow>=10.0
pillow-heif>=0.16

# Sole JS runtime for legado @js / {{}} / jsLib rules.
quickjs
